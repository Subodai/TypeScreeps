class RoomIntel {

    public saveIntel(refresh: boolean = false): {[k: string]: any} {
        let info: {[key: string]: any};
        if (refresh) {
            info = {};
        } else {
            info = {};
        }

        return info;
    }
}
